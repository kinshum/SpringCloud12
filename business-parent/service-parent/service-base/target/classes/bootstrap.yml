server:
  port: 5001

management:
  security:
    enabled: false
  health:  
    mail:  
      enabled: false
      
spring:
  application:
    name: service-base

  cloud:
    config:
      name: ${spring.application.name}
      uri: http://localhost:7001